import { IndicatorBarDirective } from './indicator-bar.directive';

describe('IndicatorBarDirective', () => {
  it('should create an instance', () => {
    const directive = new IndicatorBarDirective();
    expect(directive).toBeTruthy();
  });
});
