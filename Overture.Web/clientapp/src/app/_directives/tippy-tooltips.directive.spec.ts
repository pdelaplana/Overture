import { TippyTooltipsDirective } from './tippy-tooltips.directive';

describe('TippyTooltipsDirective', () => {
  it('should create an instance', () => {
    const directive = new TippyTooltipsDirective();
    expect(directive).toBeTruthy();
  });
});
