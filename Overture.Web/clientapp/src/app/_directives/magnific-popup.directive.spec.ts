import { MagnificPopupDirective } from './magnific-popup.directive';

describe('MagnificPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new MagnificPopupDirective();
    expect(directive).toBeTruthy();
  });
});
