import { MagnificPopupDialogDirective } from './magnific-popup-dialog.directive';

describe('MagnificPopupDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new MagnificPopupDialogDirective();
    expect(directive).toBeTruthy();
  });
});
