import { AttachmentsCarouselDirective } from './attachments-carousel.directive';

describe('AttachmentsCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new AttachmentsCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
